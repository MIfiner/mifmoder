plugins { id 'fabric-loom' version '1.2.13' id 'java' } group = 'com.yourname' version = '1.0.0' def minecraftVersion = '1.21.4' repositories { mavenCentral() maven { url = 'https://maven.fabricmc.net/' } } dependencies { minecraft "com.mojang:minecraft:${minecraftVersion}" mappings "net.fabricmc:yarn:1.21.4+build.1:v2" modImplementation "net.fabricmc:fabric-loader:0.15.3" // add other deps if needed } java { toolchain.languageVersion = JavaLanguageVersion.of(17) } tasks.withType(JavaCompile).configureEach { options.encoding = 'UTF-8' } loom { // настройка по умолчанию }
